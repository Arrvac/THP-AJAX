<div class="container">
    <div class="row">
        <h2 class="display-6"><%= email.object %></h2>
    </div>
    <div class="row">
        <p><%= email.body %></p>
        <p><%= email.read %></p>
    </div>
    <p>
            <%= link_to 'Supprimer', email_path(email), method: :delete, remote: true, class: "btn btn-secondary" %>
            <%= link_to 'Marque comme non lu', emails_path, remote: true, class: "btn btn-secondary" %>
    </p>
</div>

